name: null

channels:
  - conda-forge
  - nodefaults

dependencies:
  - jupyterlab
  - jupyterlab-lsp
  - make
  - numpy
  - openai
  - pip
  - pip:
    - -r requirements.txt
  - python
  - python-lsp-server
  - tqdm
